@using Extrade.ViewModels


@{Layout="_Layout";}
@model List<VendorViewModel>
<table class="table">
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Vendor Name</th>
      <th scope="col">Brand Arabic</th>
      <th scope="col">Brand English</th>
       <th scope="col">Remove</th>

     
    </tr>
  </thead>
  <tbody>

   
    @foreach (var p in Model)
    {
         <tr>
      <td>@p.UserID</td>
      <td>@p.NameAr</td>
      <td>@p.BrandNameAr</td>
      <td>@p.BrandNameEr</td>
      @* <td>@if (@user.IsDeleted == true)
          {<a asp-action="SoftDelete" asp-controller="User" 
          asp-route-Id="@user.ID"
          class="btn btn-primary">Not Activate</a>
          }
          else {<a asp-action="SoftDelete" asp-controller="User" 
          asp-route-Id="@user.ID"
          class="btn btn-danger">Active</a>
          }
                    
                </td>*@
    </tr>
        
    }
   
  </tbody>
</table>